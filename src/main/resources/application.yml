spring:
  application:
    name: ridenow

  datasource:
    url: jdbc:mysql://localhost:3306/rnc?useSSL=false
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update


